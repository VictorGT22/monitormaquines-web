import { defineConfig } from 'vite'

// Sense React: aquesta app és el codi ORIGINAL de MonitorMaquines
// (index.html/styles.html) tal qual — Vite només fa de servidor de
// desenvolupament i d'empaquetador per a Electron/Capacitor.
export default defineConfig({})
